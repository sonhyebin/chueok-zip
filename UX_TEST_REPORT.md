# 추억.zip — UX 자동 테스트 리포트 (1차)

> 이 리포트는 Claude가 Playwright로 배포된 사이트를 실제 사용자처럼 조작하며 작성했습니다.
> 스크린샷은 `screenshots/ux-test/`에 있으며, 이 단계에서는 **어떤 코드도 수정하지 않았습니다.**
> 재실행 방법: `node scripts/ux-test.mjs`

## Test environment

| 항목 | 값 |
|---|---|
| URL | https://2000-steel.vercel.app (프로덕션 배포본) |
| 테스트 일시 | 2026-08-20 |
| 자동화 도구 | Playwright 1.62.1 (headless Chromium) |
| 디바이스 프로필 | iPhone 13 에뮬레이션 (모바일 UA, 터치, DPR 3) |
| 기본 viewport | 390 × 844 |
| 추가 viewport | 375×667 / 393×852 / 430×932 (레이아웃 오버플로 검사만) |

제약: headless Chromium은 실제 iOS Safari(WebKit)가 아니며 `navigator.share`(네이티브 공유 시트)를 지원하지 않음. 공유는 클립보드 폴백 경로로 검증했고, 네이티브 공유 시트·실제 카카오톡 인앱 동작은 실기기 확인이 필요함.

## Tested flow

1. 홈 접속 → 출생연도 `1992` 입력 → CTA 클릭
2. 타임라인(1992년생) → 2005년 선택
3. 2005 추억 피드 스크롤 (카테고리 연속 여부·오버플로 검사)
4. 친구 공유 CTA → 타임캡슐 작성 진입
5. 사용자 A(혜빈): 이름 + 5문항 자연어 답변 → 초대 링크 생성 → 공유(클립보드 폴백)
6. **새 브라우저 컨텍스트**(localStorage 격리)에서 사용자 B(수진)가 초대 링크 진입 → A 답변 비노출 확인 → 5문항 답변 → 제출
7. 결과 페이지 검증 (제목, 답변 병합, 재공유 CTA)
8. `/?born=1992` 직접 진입 → 타임라인 자동 이동
9. localStorage 유지(새로고침) / 삭제 후 신규 사용자 상태
10. 전 과정 console error / page error / network 실패 수집

## Passed (자동 체크 33개 중 31개 통과)

- 홈 로딩·핵심 질문 노출·가로 오버플로 없음
- 입력 전 CTA 비활성 → 4자리 입력 시 활성 → 타임라인 이동
- 1992년생 타임라인: 2003~2010 표시, 나이 병기, 준비중/활성 구분, 2005 "★ 추천" 배지
- 2005 피드: 나이 계산 정확(13살), 카드 16장에서 **동일 카테고리 연속 등장 0건**, 가로 오버플로 없음
- 친구 공유 CTA 노출(피드 중간 + 하단)
- 캡슐 A: 5문항 전부 입력 시에만 제출 활성, 공유 준비 화면, 링크 복사 폴백 + "복사 완료" 피드백
- 캡슐 B: 초대 랜딩 문구, **A 답변 제출 전 비노출 확인**, 숨김 안내 문구
- 결과: "혜빈 × 수진의 2005" 제목, A/B 답변 5쌍 모두 정상 병합, 완성 메시지, 재공유 CTA
- `/?born=1992` 직접 진입 → 타임라인 자동 이동 (localStorage 충돌 없음)
- localStorage: 새로고침 후 1992 유지 / 삭제 후 신규 상태 정상
- 4개 viewport 모두 가로 오버플로 없음

자동 체크 실패 2건 중 1건("당시 나이 문구")은 테스트 스크립트의 로케이터 문제였고 스크린샷으로 정상 확인됨. 나머지 1건은 아래 M-1(조사 처리)로 이어짐.

## UX observations (수정하지 않고 기록만 함)

### High

- **H-1. A(캡슐을 만든 사람)는 결과를 자동으로 볼 수 없음.** 타임캡슐이 URL 인코딩 기반이라 B가 완성한 결과 링크를 A에게 다시 보내줘야만 A가 결과를 봄. 결과 화면에 "혜빈님에게도 결과를 보내주세요" 안내는 있으나, B가 재공유하지 않으면 A의 경험은 "보내고 끝"으로 끊김. 바이럴 루프의 핵심 구간이므로 기획 검토 필요. (구조적 이슈 — 서버 저장 전환 시 해결 가능)

### Medium

- **M-1. 한국어 조사 처리 어색.** 결과 페이지 라벨이 "혜빈**이** 기억하는 첫 만남", "수진**이** 기억하는 우리의 노래"로 표시됨. 자연스러운 표현은 "혜빈**이가**/수진**이가**"임 (받침 있는 이름에 '이'만 붙고 주격조사 '가'가 빠짐). `lib/josa.ts`의 `nameIga()` 로직. → `08-capsule-result.png`
- **M-2. 홈 입력창의 placeholder "1992"가 실제 입력된 값처럼 보임.** 큰 픽셀 폰트의 회색 1992가 이미 입력된 것으로 오인될 수 있어, 첫 방문자가 CTA만 누르고 반응이 없다고 느낄 가능성. → `01-home.png`
- **M-3. 초대 랜딩 제목 줄바꿈 어색.** "혜빈이 / 2005년에서 타임캡슐을 보냈어 / 요" — 마지막 글자 "요"만 다음 줄로 떨어짐 (390px 기준). → `06-capsule-invite.png`
- **M-4. placeholder 이미지 내부 텍스트 중복·겹침.** 임시 이미지 안의 제목 텍스트가 카드 제목과 중복되고, "임시 이미지 · 실제 사진으로 교체 예정" 문구가 주황 날짜 스탬프와 겹쳐 "교체 예정2005.01.21"처럼 붙어 보임. 실제 사진 교체 시 자연 해소되지만 현재 상태로 사용자 테스트를 하면 인상을 해칠 수 있음. → `05-share-cta.png`
- **M-5. 중간 공유 CTA 도달까지 스크롤이 김.** 카드 8장(BGM 포함 화면 약 9~10개 분량)을 지나야 첫 CTA가 나옴. "친구 생각날 때 바로 보내는" 감정 타이밍과 맞는지 기획 검토 필요. → `05-share-cta.png`

### Low

- **L-1. 결과 페이지 카드 순차 등장 애니메이션(총 ~1초).** 로드 직후 빠르게 스크롤하면 아직 등장 전(투명)인 카드 구간이 잠깐 보일 수 있음. 자동화 캡처에서 재현됨(재캡처로 확인, 1초 후 완전 정상).
- **L-2. 홈 CTA의 비활성 상태 구분이 약함.** 비활성(45% 투명 핑크)과 활성의 차이가 크지 않아 "눌러도 되는지"가 흐림. M-2와 결합 시 혼란 가능.
- **L-3. BGM 플레이어의 재생바가 장식용(0:00 고정).** 실제 재생 진행과 무관해 눌러보는 사용자가 있을 수 있음.
- **L-4. 타임라인에서 준비중 연도가 절반 이상(1992년생 기준 8칸 중 3칸).** 데이터 확장 전까지는 자연스러운 상태이나 첫인상에 "비어 보이는" 느낌을 줄 수 있음.

## Technical issues

- **console error: 0건 / page error(uncaught exception): 0건 / hydration error: 0건** — 전체 플로우에서 깨끗함
- **network 실패 3건: 모두 무해.** `/year/2006?_rsc=…`, `/year/2007?_rsc=…` ERR_ABORTED — Next.js Link prefetch가 페이지 이동으로 중단된 것 (사용자 영향 없음)
- 404 asset / 이미지 로드 실패: 0건 (폰트·placeholder SVG·OG 모두 정상)
- `navigator.share`: headless Chromium 미지원 → 클립보드 폴백 정상 동작 확인. **실기기(iOS Safari/카카오톡 인앱)의 네이티브 공유 시트는 별도 확인 필요**
- 초대 URL 길이: **360자** (한국어 답변 5개 포함) — 카카오톡 전송 가능 범위

## Screenshots (`screenshots/ux-test/`)

| 파일 | 내용 |
|---|---|
| `01-home.png` | 홈 — 출생연도 입력 (placeholder 1992 상태) |
| `02-timeline-1992.png` | 1992년생 타임라인 (2005 ★추천 배지, 준비중 구분) |
| `03-2005-feed-top.png` | 2005 피드 상단 — 연도/나이 헤더 + BGM 플레이어 + 첫 카드 |
| `04-2005-feed-middle.png` | 피드 중간 스크롤 지점 |
| `05-share-cta.png` | 친구 공유 CTA 직전~노출 구간 (M-4 겹침도 이 화면에서 확인) |
| `06-capsule-invite.png` | B의 초대 랜딩 (M-3 줄바꿈 확인 가능) |
| `07-capsule-form.png` | B의 답변 작성 화면 (5문항 입력 완료 상태) |
| `08-capsule-result.png` | 결과 전체 (fullPage, 애니메이션 완료 후 재캡처) — M-1 조사 확인 가능 |

## 다음 단계 제안 (수정은 하지 않음)

우선순위 후보: H-1(결과 전달 루프)은 기획 결정 필요, M-1(조사)·M-3(줄바꿈)·M-4(placeholder 겹침)는 저비용 수정. 실기기에서 공유 시트·BGM 재생(iOS 정책상 두 번 탭)·카카오톡 인앱 진입은 사람이 직접 확인 권장.
